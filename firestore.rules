rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development mode: allow all reads and writes to eliminate permission errors.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
