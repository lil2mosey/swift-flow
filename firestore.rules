rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary bypass to ensure development progress and resolve sync issues
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}