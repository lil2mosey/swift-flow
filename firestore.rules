rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary broad access for authenticated users to ensure role-based logic works
    // and data is accessible across navigation.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}